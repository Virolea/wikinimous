<div class="container">
  <h1><%= @article.title%></h1>
  <hr>
  <p>
    <%= @markdown.render(@article.content).html_safe %>
  </p>
  <%= link_to "éditer", edit_article_path %>
  <%= link_to "Supprimer", article_path(@article), method: :delete, data: { confirm: "Are you sure you want to delete this article?"} %>
</div>

